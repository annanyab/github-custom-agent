# Agent Orchestrator Workspace

Welcome to the **Agentic Infrastructure Hub**. This repository utilizes a "Component-Based" AI architecture to manage the full lifecycle of digital transformation—from business requirements to production-ready Azure infrastructure.

## 🏗️ Architecture Design

We follow a strict **Separation of Concerns** between the **Logic Plane** (how agents think) and the **Documentation Plane** (what agents produce).



### 1. Logic Plane (`.github/agents/`)
Each agent is a self-contained unit. This ensures that skills, instructions, and benchmarks are scoped specifically to the agent's persona, preventing "context leaking" and improving precision.

* **`agent.md`**: The identity, YAML frontmatter, and tool definitions.
* **`instructions/`**: Domain-specific guardrails, architectural standards, and compliance rules.
* **`skills/`**: Executable capabilities (Python/JS scripts) unique to the agent.
* **`evals/`**: Scoring rubrics used by the "Judge" logic to validate output quality.
* **`benchmark/`**: "Golden samples" used as a reference to ensure consistency.

### 2. Documentation Plane (`docs/agents/`)
This is the "Audit Trail." It serves as the physical storage for all artifacts generated by the AI workforce.
* **`input/`**: Upstream context, business requirements, or handoff files.
* **`output/`**: Final artifacts including User Stories, Terraform HCL, and Mermaid diagrams.

---

## 🤖 Active Agent Directory

| Agent | Role | Responsibility |
| :--- | :--- | :--- |
| **`create-story`** | Business Analyst | Transforms high-level intent into structured user stories in `docs/`. |
| **`generate-code`** | Technical Architect | Converts stories into Azure-compliant HCL and application logic. |

---

## 📁 Repository Structure

```text
.
├── .github/
│   └── agents/
│       ├── create-story/           # Logic: Storytelling Specialist
│       │   ├── agent.md
│       │   ├── benchmark/
│       │   ├── evals/
│       │   ├── instructions/
│       │   └── skills/
│       └── generate-code/          # Logic: Coding Specialist
│           ├── agent.md
│           ├── benchmark/
│           ├── evals/
│           ├── instructions/
│           └── skills/
├── docs/
│   └── agents/                     # Artifact Registry (Documentation Plane)
│       ├── create-story/
│       │   ├── input/
│       │   └── output/             # Story artifacts live here
│       └── generate-code/
│           ├── input/
│           └── output/             # Terraform/App code lives here
└── README.md
